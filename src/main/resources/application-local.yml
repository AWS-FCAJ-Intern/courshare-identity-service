spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/identity_db
    username: courshare
    password: courshare
  jpa:
    hibernate:
      ddl-auto: none
    open-in-view: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
  data:
    redis:
      host: localhost
      port: 6379
